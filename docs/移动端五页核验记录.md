# 51电子网后台移动端五页核验记录

核验日期为 **2026-07-31**，使用本地开发服务与 Chromium 移动设备模拟，视口为 **390 × 844**，页面实际 `window.innerWidth` 为 **390px**。核验期间创建的一次性超级管理员账号及临时脚本均已删除，项目数据库不保留已知测试凭证。

| 页面 | DOM/文本核验 | 实际交互结果 | 结论 |
|---|---|---|---|
| `/login` | 用户名输入框、密码输入框、登录按钮、忘记用户名与忘记密码入口均可见 | 填写账号密码并提交后，URL 从 `/login` 回跳至 `/` | 通过 |
| `/` 物料数据库 | 搜索框占位文字完整；搜索按钮可见；分类与生命周期共 2 个下拉控件 | 打开分类下拉后显示“全部分类”；移动侧边栏可展开 | 通过 |
| `/merchants` 商户管理 | 公司名称/商户编号搜索框、搜索按钮、状态筛选和空状态均可见 | 打开状态筛选后显示“全部状态、待审核、待补件、已入驻、已暂停” | 通过 |
| `/messages` 消息中心 | 主题/联系人搜索框、类型与状态共 2 个筛选控件、空状态均可见 | 打开类型筛选后显示“全部类型、快速询价、在线客服、普通留言” | 通过 |
| `/admins` 后台用户管理 | “新建用户”、用户账户列表及分页内容可见 | 打开新建用户对话框后，用户名、显示名称、手机号、邮箱、密码字段及“创建用户”动作均可达 | 通过 |

移动侧边栏展开后，**物料数据库、商户管理、消息中心、后台用户管理**四个入口均可见。页面同时完成了桌面视口截图核验与移动视口截图核验；移动端物料搜索框、页头动作区与后台用户空状态的响应式问题已修复并复查。
