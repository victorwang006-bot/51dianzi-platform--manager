# Project TODO

- [x] 从 `victorwang006-bot/51dianzi-platform--manager` 安全克隆源码，并记录导入提交版本
- [x] 将仓库源码合并至当前 `web-db-user` 项目，保留当前托管环境必需的项目配置
- [x] 核验统一侧边栏包含物料数据库、商户管理、消息中心、后台用户管理四个入口
- [x] 核验物料列表、搜索筛选、详情查看、上架与必填原因下架流程
- [x] 核验商户列表、商户详情及内嵌客户物料面板
- [x] 核验商户统一社会信用代码与前台物料数据的关联逻辑及不可用提示
- [x] 核验消息列表、前后台消息收发及已读/未读状态管理
- [x] 核验管理员列表及新增、编辑、禁用后台用户流程
- [x] 核验角色权限控制、后台专属登录页、受保护路由与未授权重定向
- [x] 校准数据库结构、运行环境变量与本地预览兼容性，不写入生产凭证
- [x] 配置并验证 `VITE_BASE_PATH=/admin/` 构建及 Nginx 子路径路由兼容性
- [x] 更新或补充 Vitest 单元测试，覆盖导入后的关键权限与业务逻辑
- [x] 运行依赖安装、Vitest、TypeScript 类型检查与生产构建
- [x] 在桌面与移动视口核验登录页及核心后台页面视觉与交互
- [x] 整理导入版本、验证结果、环境限制及后续生产部署说明
- [x] 建立后台角色权限矩阵，按模块与读写动作限制服务端接口
- [x] 在侧边栏与前端路由同步执行角色权限，非授权角色不可进入受限模块
- [x] 将后台用户管理限定为超级管理员，并增加对应 Vitest 权限测试
- [x] 新增公开 `/login` 路由，未登录访问受保护页面时执行 URL 级重定向
- [x] 登录成功后返回原目标页面，并补充独立的重定向逻辑单元测试
- [x] 修复移动端后台用户页“新建用户”按钮横向溢出及空状态文案贴边
- [x] 修复移动端物料筛选区搜索输入框过窄，保证占位文字与输入内容可用
- [x] 在已授权桌面会话核验 `/messages` 与 `/admins` 的可见内容、筛选及新增入口
- [x] 在已授权移动会话核验 `/messages` 与 `/admins` 的 DOM 内容与关键交互可达性
- [x] 在移动浏览器会话记录 `/messages` 与 `/admins` 页面文本，并实际操作导航、筛选控件和新增用户入口
- [x] 在 390px 移动浏览器核验 `/login` 页面 DOM、账号密码输入与登录回跳
- [x] 在 390px 移动浏览器核验物料页搜索输入、搜索按钮、分类与生命周期筛选控件
- [x] 在 390px 移动浏览器核验商户页搜索、状态筛选与空状态/列表内容
- [x] 整理覆盖登录、物料、商户、消息、后台用户五页的移动 DOM 与关键交互核验记录
- [x] 核对阿里云 ECS SSH 连接、线上目录、Node/pnpm/PM2/Nginx 与当前运行版本
- [x] 在 ECS 创建带时间戳的代码、运行配置与 Nginx 配置备份，记录回滚路径
- [x] 审计生产环境变量与数据库连通性，不覆盖或输出生产密钥
- [x] 将项目检查点 931ba7e2 对应源码（Git/部署提交 f0ddfdc）安全同步到 ECS，保留 uploads 与生产环境文件
- [x] 在 ECS 安装锁定依赖并执行 `pnpm check`、`pnpm build:admin` 与 `/admin/` 构建自检
- [x] 校验 Nginx 配置并平滑重启后台 PM2 进程
- [x] 验证生产 `/admin/login`、静态资源、tRPC、后台登录与四个核心模块
- [x] 只读验证前台跨库查询与下架 SQL 权限/执行计划未受部署影响，不修改真实生产物料
- [x] 检查 PM2、Nginx 与应用错误日志，确认无新增 5xx 或启动异常
- [x] 记录生产部署版本、备份位置、验证结果与可执行回滚命令
- [x] 确认当前工作区以检查点 931ba7e2 为业务源码基线，并包含部署记录更新且无临时密钥或测试脚本
- [x] 创建本次发布 Git 提交并记录本地提交哈希
- [x] 使用临时认证将提交安全推送到 `victorwang006-bot/51dianzi-platform--manager` 的 `main` 分支
- [x] 生成排除 `.git`、日志、`dist`、`node_modules`、`.env`、私钥和测试产物的版本化部署包
- [x] 为部署包生成 SHA-256 校验值与发布清单，并在上传后于 ECS 复核一致性
- [x] 部署记录完成后保存新的项目检查点，固化当前工作区与最终生产版本关系
- [x] 在生产部署记录中明确 checkpoint `931ba7e2`、Git `f0ddfdc` 与 ECS 发布清单的映射
- [x] 核实前台库现有 464 条已发布物料，但后台当前无统一社会信用代码匹配且有物料的商户，记录为数据前置条件而非部署故障
- [x] 未经用户额外批准不对真实已发布物料执行下架与恢复写入测试
- [x] 只读核对正式 GitHub main、阿里云活动后台 release、“商城订单”现有实现与发布脚本，确认本次最小修改基线（GitHub `e6b1391d59f9`；生产 `20260805T090253Z-admin-crm-secret-free-v2`）
- [x] 根据前台订单只读契约实现仅服务端持有共享密钥的订单代理路由，禁止浏览器接触密钥且不复制或双写订单
- [x] 在现有商城订单页面接入真实前台订单，展示订单号、买家、商家、商品明细、金额、状态和时间，并提供分页、搜索、空态与错误态
- [x] 为商城订单读取增加服务端与前端角色权限边界，未授权角色不得调用接口或进入页面
- [x] 补充无密钥拒绝、上游异常、分页搜索、状态映射、角色权限和页面契约 Vitest 回归（12 项订单相关回归通过）
- [x] 执行后台与改动相关的回归、TypeScript 检查、`/admin/` 生产构建和构建自检（12 项订单回归、TypeScript、生产构建及 `/admin/` 自检通过；全量回归测试库漂移例外经用户批准）
- [x] 为后台订单代理显式断言 `page/pageSize/keyword` 转发、状态字段及买家/商家关联字段映射，避免仅凭示例数据误判覆盖
- [x] 执行并审计后台全量 Vitest：沙箱测试库返回 `ER_NO_SUCH_TABLE 1146`（缺少 `merchants` 表），用户明确批准沿用“仅相关回归”口径继续本批
- [x] 只读识别当前沙箱测试数据库的库名、现存表清单与环境归属，不输出连接凭据或业务数据
- [x] 检索测试、迁移和脚本对候选测试表的引用及外键依赖，判断删除是否会破坏后续回归或误触生产结构
- [x] 向用户报告可删除对象、不可删除对象与推荐清理方式；结论为 `users` 和 `__drizzle_migrations` 均不可删表，当前也不存在 `merchants` 表，未执行任何删除
- [x] 显式核对迁移文件、迁移执行脚本和测试清理代码对 `users`、`__drizzle_migrations` 的引用与恢复语义
- [x] 将测试库删除安全评估文档交付给用户，并明确若仅清理废弃账号须再次确认具体记录后定点删除
- [x] 审阅完整 todo.md、Git 差异和发布白名单，确保发布包不含密钥、环境、数据库、日志、uploads、node_modules、旧 dist 或审计产物
- [x] 基于现有原子发布模式新增订单只读任务专用发布脚本与契约测试，固定当前后台/前台活动基线、最小源码白名单、双实例切换和失败回滚（脚本语法、4 项发布契约、12 项订单回归及 TypeScript 通过）
- [x] 修复订单原子脚本对 esbuild 服务端 bundle 的脆弱字面量检查，改为源码路由契约、生产 UI 标记和真实只读代理烟测联合门禁（脚本语法、4 项发布契约、12 项订单回归和 TypeScript 通过）
- [x] 重跑脚本语法、发布契约、订单相关回归与 TypeScript，生成新 release ID 并仅重试一次后台原子发布
- [x] 后台重试成功后只读核验新活动 release、双实例、前台四实例、Nginx、登录页和商城订单只读代理，并清理本次失败的未激活 release/上传包
- [x] 前台订单接口生产验收通过后，因既有任务脚本硬编码旧活动基线，基于其原子发布模式新增并使用订单只读专用脚本发布后台至阿里云 ECS，完成后台双实例、前台四实例、Nginx 与商城订单页面核验（release `20260805T154206Z-admin-order-readonly-v2`）
- [x] 后台生产发布成功后向正式后台 GitHub main 普通追加推送，禁止 force push（commit `553818f82754a4898180c31373a2289f19ac638d`）
- [x] 从正式后台 GitHub main 与沙箱自动恢复副本逐文件重建 CRM 企业/银行字段、迁移和专用原子发布脚本
- [x] 恢复后重跑后台相关回归、TypeScript、`/admin/` 生产构建与发布白名单，确认与重置前门禁结果一致（37 项相关回归、TypeScript、生产构建、`/admin/` 自检和 12 文件白名单通过）
- [x] 重建后重新生成后台 CRM 字段最小发布包并逐项比对专用脚本 `EXPECTED_LIST`、敏感路径排除和精确文件清单
- [x] 在后台商户模型新增可空企业类型和企业角色列，并复用既有账户名称、账户号码、开户行三列
- [x] 后台 `portal.submitCrmApplication` 强制企业与银行字段必填，并在创建、认领、重申请和同账号重复提交时统一同步
- [x] 后台商户/CRM 详情按独立栏展示企业类型、企业角色、账户名称、账户号码和开户行
- [x] 生成并收敛后台最小迁移 `0018_nifty_purple_man.sql`，仅做两个标准可空加列；发布脚本通过列、账本和商户行数严格预检
- [x] 执行后台 CRM 字段、迁移、发布、权限与授权策略相关回归、TypeScript、`/admin/` 构建、自检和 12 文件发布白名单（37 项回归通过）
- [x] 使用专用原子脚本单次重试并成功发布后台 CRM 字段能力，完成双实例、Nginx、迁移账本、目标列、9 条商户行数与冻结前台验收（release `20260806T010223Z-admin-crm-bank-fields-v2`）
- [x] 清理首次失败的未激活后台候选 release、上传归档、失败备份与本地发布包，保留当前活动 release 和必要回滚基线
- [x] 后台生产验收完成后普通追加提交并推送正式 GitHub main，禁止 force push（业务提交 `19bc04a41f3c2920d63e048b3ef996ea759a5d95`）
